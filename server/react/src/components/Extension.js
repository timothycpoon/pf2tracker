import React from 'react';

const Extension = () => (
    <div>
        Download the extension <a href="/static/pf2tracker-parser.zip">here.</a> Unzip the file, enable developer mode on Chrome, and click "Load Unpacked".
    </div>
);

export default Extension;
